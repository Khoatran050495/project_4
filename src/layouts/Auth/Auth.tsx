import React from "react";

import Login from "../../Components/Login/Login";

type Props = {};

const Auth: React.FC = (props: Props) => {
  return (
    <div>
      <Login />
    </div>
  );
};

export default Auth;
